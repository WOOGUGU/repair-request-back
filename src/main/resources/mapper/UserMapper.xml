<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkkoke.networkrepair.dao.UserDao">
    <insert id="addUser" parameterType="com.kkkoke.networkrepair.pojo.User">
        insert into user(username, password, name)
        values (#{username}, #{password}, #{name})
    </insert>
    
    <delete id="deleteUser" parameterType="long">
        delete
        from user
        where id = #{id}
    </delete>

    <select id="selectUserByUsername" parameterType="string" resultType="com.kkkoke.networkrepair.pojo.User">
        select *
        from user
        where username = #{username}
    </select>

    <select id="selectUserById" parameterType="long" resultType="com.kkkoke.networkrepair.pojo.User">
        select *
        from user
        where id = #{id}
    </select>

    <select id="selectAllUser" resultMap="userMap">
        select *
        from user
    </select>

    <update id="updateUser">
        update user
        set `username` = #{username},
            `password` = #{password},
            `name`     = #{name}
        where id = #{id}
    </update>

    <select id="loadUserByUsername">
        select *
        from user
        where username = #{username}
    </select>

    <select id="getRolesByUid">
        select r.id, r.name, r.name_zh nameZh
        from role r, user_role ur
        where r.id = ur.rid and ur.uid = #{uid}
    </select>

    <resultMap id="userMap" type="com.kkkoke.networkrepair.pojo.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
    </resultMap>
</mapper>